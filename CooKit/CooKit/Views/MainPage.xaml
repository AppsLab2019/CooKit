<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:views="clr-namespace:CooKit.Views"
             xmlns:viewmodels="clr-namespace:CooKit.ViewModels"
             mc:Ignorable="d"
             x:Class="CooKit.Views.MainPage">

    <ContentPage.BindingContext>
        <viewmodels:MainPageViewModel />
    </ContentPage.BindingContext>
    
    <ContentPage.Content>
        <CollectionView ItemsSource="{Binding Recipes}"
                        RemainingItemsThreshold="5"
                        RemainingItemsThresholdReachedCommand="{Binding ThresholdReachedCommand}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <views:RecipePreview Image="{Binding Image}" 
                                         Name="{Binding Name}" 
                                         HeightRequest="200"
                                         Padding="20,20,20,0"/>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </ContentPage.Content>
</ContentPage>