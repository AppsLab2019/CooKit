<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:m="clr-namespace:CooKit.Models;assembly=CooKit"
             xmlns:vme="clr-namespace:CooKit.ViewModels.Editor;assembly=CooKit"
             mc:Ignorable="d"
             x:Class="CooKit.Views.Editor.RecipeDesignerView">

    <ContentPage.BindingContext>
        <vme:RecipeDesignerViewModel/>
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <Grid Padding="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Entry IsReadOnly="True"
                   Placeholder="Id"
                   Text="{Binding Id}"/>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Entry Placeholder="Name"
                       Text="{Binding Name}"/>

                <TimePicker Grid.Column="1"
                            Format="HH:mm"
                            Time="{Binding RequiredTime}"/>
            </Grid>

            <Editor Grid.Row="2"
                    HeightRequest="80"
                    Placeholder="Description"
                    Text="{Binding Description}"/>

            <Grid Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Picker Title="Image Loader"
                        SelectedItem="{Binding ImageLoader}"
                        ItemsSource="{Binding AvailableImageLoaders}"/>

                <Entry Grid.Column="1"
                       Placeholder="Image Source"
                       Text="{Binding ImageSource}"/>
            </Grid>

            <CollectionView Grid.Row="4"
                            SelectionMode="Multiple"
                            ItemsSource="{Binding AllIngredients}"
                            SelectedItems="{Binding SelectedIngredients}">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="m:IIngredient">
                        <Grid Padding="6">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Image Grid.RowSpan="2"
                                   Source="{Binding Icon}"/>

                            <Label Grid.Row="0"
                                   Grid.Column="1"
                                   FontSize="Medium"
                                   Text="{Binding Name}"/>

                            <Label Grid.Row="1"
                                   Grid.Column="1"
                                   FontSize="Small"
                                   Text="{Binding Id}"/>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <CollectionView Grid.Row="5"
                            SelectionMode="Multiple"
                            ItemsSource="{Binding AllPictograms}"
                            SelectedItems="{Binding SelectedPictograms}">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="m:IPictogram">
                        <Grid Padding="6">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Image Grid.RowSpan="2"
                                   Source="{Binding Image}"/>

                            <Label Grid.Row="0"
                                   Grid.Column="1"
                                   FontSize="Medium"
                                   Text="{Binding Name}"/>

                            <Label Grid.Row="1"
                                   Grid.Column="1"
                                   FontSize="Small"
                                   Text="{Binding Id}"/>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Button Grid.Row="6"
                    Text="Edit Steps"
                    Command="{Binding EditStepsCommand}"/>

            <Button Grid.Row="7"
                    Text="Create"
                    Command="{Binding CreateCommand}"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>