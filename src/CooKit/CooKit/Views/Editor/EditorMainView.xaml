<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="clr-namespace:XF.Material.Forms.UI;assembly=XF.Material"
             xmlns:forms="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             xmlns:models="clr-namespace:CooKit.Models;assembly=CooKit"
             xmlns:vm="clr-namespace:CooKit.ViewModels;assembly=CooKit"
             xmlns:editor="clr-namespace:CooKit.ViewModels.Editor;assembly=CooKit"
             mc:Ignorable="d"
             x:Class="CooKit.Views.Editor.EditorMainView"
             x:DataType="editor:EditorMainViewModel"
             vm:ViewModelLocator.AutoWireVM="True"
             Style="{StaticResource Style.Page}">

    <ContentPage.Resources>
        <ResourceDictionary>
            <Style x:Key="Style.FloatingCard" TargetType="ui:MaterialCard"
                   BasedOn="{StaticResource Style.SurfaceCard}">
                <Setter Property="CornerRadius" Value="5"/>
            </Style>

            <Style x:Key="Style.FloatingClickableCard" TargetType="ui:MaterialCard"
                   BasedOn="{StaticResource Style.FloatingCard}">
                <Setter Property="IsClickable" Value="True"/>
            </Style>

            <Style x:Key="Style.SealedCard" TargetType="ui:MaterialCard">
                <Setter Property="Padding" Value="0"/>
                <Setter Property="Elevation" Value="0"/>
                <Setter Property="IsClickable" Value="True"/>
                <Setter Property="BackgroundColor" Value="{StaticResource Color.Surface}"/>
            </Style>
        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.Content>
        <ScrollView>
            <StackLayout Spacing="10">
                <ui:MaterialCard Padding="0"
                                 Elevation="2"
                                 BackgroundColor="{StaticResource Color.Background}">
                    <Grid HeightRequest="250">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="0.01*"/>
                        </Grid.RowDefinitions>

                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="4*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <CarouselView Grid.RowSpan="3"
                                      Grid.ColumnSpan="6"
                                      IndicatorView="ImageIndicator"
                                      ItemsSource="{Binding Images}">
                            <CarouselView.ItemTemplate>
                                <DataTemplate x:DataType="x:Uri">
                                    <forms:CachedImage Source="{Binding}"
                                                       Aspect="AspectFill"
                                                       DownsampleToViewSize="True"/>
                                </DataTemplate>
                            </CarouselView.ItemTemplate>
                        </CarouselView>

                        <IndicatorView Grid.Row="1"
                                       Grid.Column="1"
                                       VerticalOptions="End"
                                       x:Name="ImageIndicator"
                                       SelectedIndicatorColor="White"
                                       IndicatorColor="LightGray"/>

                        <ui:MaterialIconButton Grid.Row="1"
                                               Grid.Column="3"
                                               ButtonType="Text"
                                               WidthRequest="32"
                                               HeightRequest="32"
                                               TintColor="White"/>

                        <ui:MaterialIconButton Grid.Row="1"
                                               Grid.Column="4"
                                               ButtonType="Text"
                                               WidthRequest="32"
                                               HeightRequest="32"
                                               TintColor="White"/>

                        <ui:MaterialIconButton Grid.Row="1"
                                               Grid.Column="5"
                                               ButtonType="Text"
                                               WidthRequest="32"
                                               HeightRequest="32"
                                               TintColor="White"/>
                    </Grid>
                </ui:MaterialCard>

                <Grid Margin="20,0,20,0"
                      HeightRequest="40">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <ui:MaterialCard Padding="4"
                                     Style="{StaticResource Style.FloatingClickableCard}">
                        <StackLayout Orientation="Horizontal">
                            <ui:MaterialIcon Source="{StaticResource Icon.Timer}"
                                             TintColor="{StaticResource Color.OnSurface}"/>

                            <ui:MaterialLabel Margin="0,0,5,0"
                                              VerticalOptions="Center"
                                              Style="{StaticResource FontStyle.Body}"
                                              TextColor="{StaticResource Color.OnSurface}">
                                <ui:MaterialLabel.FormattedText>
                                    <FormattedString>
                                        <Span Text="{Binding EstimatedTime}"/>
                                        <Span Text=" min."/>
                                    </FormattedString>
                                </ui:MaterialLabel.FormattedText>
                            </ui:MaterialLabel>
                        </StackLayout>
                    </ui:MaterialCard>

                    <ui:MaterialCard Grid.Column="1"
                                     Padding="4"
                                     Style="{StaticResource Style.FloatingClickableCard}">
                        <CollectionView ItemsSource="{Binding Pictograms}">
                            <CollectionView.ItemsLayout>
                                <LinearItemsLayout Orientation="Horizontal"
                                                   ItemSpacing="5"/>
                            </CollectionView.ItemsLayout>

                            <CollectionView.ItemTemplate>
                                <DataTemplate x:DataType="models:Pictogram">
                                    <ui:MaterialIconButton WidthRequest="32"
                                                           HeightRequest="32"
                                                           Image="{Binding Icon}"/>
                                </DataTemplate>
                            </CollectionView.ItemTemplate>
                        </CollectionView>
                    </ui:MaterialCard>
                </Grid>

                <ui:MaterialCard Margin="20,0,20,0"
                                 Style="{StaticResource Style.FloatingCard}">
                    <StackLayout>
                        <ui:MaterialCard Style="{StaticResource Style.SealedCard}">
                            <ui:MaterialLabel Text="{Binding Name}"
                                              TextColor="{StaticResource Color.OnSurface}"
                                              Style="{StaticResource FontStyle.SmallHeadline}"/>
                        </ui:MaterialCard>

                        <BoxView HeightRequest="2"
                                 BackgroundColor="#F0F0F0F0"/>

                        <ui:MaterialCard Style="{StaticResource Style.SealedCard}">
                            <ui:MaterialLabel Text="{Binding Description}"
                                              TextColor="{StaticResource Color.OnSurface}"
                                              Style="{StaticResource FontStyle.SecondBody}"/>
                        </ui:MaterialCard>
                    </StackLayout>
                </ui:MaterialCard>

                <ui:MaterialCard Margin="20,0,20,0"
                                 Style="{StaticResource Style.FloatingCard}">
                    <StackLayout>
                        <ui:MaterialLabel Text="You'll need:"
                                          Style="{StaticResource FontStyle.SecondSubtitle}"
                                          TextColor="{StaticResource Color.OnSurface}"/>

                        <StackLayout BindableLayout.ItemsSource="{Binding Ingredients}">
                            <BindableLayout.ItemTemplate>
                                <DataTemplate x:DataType="models:Ingredient">
                                    <StackLayout Padding="4"
                                                 Spacing="10"
                                                 HeightRequest="40"
                                                 Orientation="Horizontal">
                                        <forms:CachedImage WidthRequest="32"
                                                           HeightRequest="32"
                                                           Source="{Binding Icon}"
                                                           DownsampleToViewSize="True"/>

                                        <ui:MaterialLabel Text="{Binding Name}"
                                                          VerticalOptions="Center"
                                                          Style="{StaticResource FontStyle.Body}"
                                                          TextColor="{StaticResource Color.OnSurface}"/>

                                        <ui:MaterialIconButton ButtonType="Text"
                                                               WidthRequest="32"
                                                               HeightRequest="32"
                                                               VerticalOptions="Center"
                                                               TintColor="{StaticResource Color.OnSurface}"/>
                                        <!--Image="{StaticResource Icon.Add}"-->
                                        <!--TODO: Replace with path-->
                                    </StackLayout>
                                </DataTemplate>
                            </BindableLayout.ItemTemplate>
                        </StackLayout>
                    </StackLayout>
                </ui:MaterialCard>

                <ui:MaterialCard Margin="20,0,20,10"
                                 Style="{StaticResource Style.FloatingCard}">
                    <ui:MaterialCard Elevation="2" 
                                     WidthRequest="200" 
                                     HeightRequest="200"
                                     BackgroundColor="{StaticResource Color.Surface}"/>
                </ui:MaterialCard>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>